<h1>All Events</h1>
<% @events.each do |event| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
       <%= link_to event.title, event %>
      </h4>
    </div>
  </div>
<% end %>