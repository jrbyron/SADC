<div class="media" id="comment-<%= comment.id %>">
  <%= link_to '#', class: 'pull-left' do %>
    <%= image_tag(comment.user.avatar.small.url) if comment.user.avatar? %>
  <% end %>
  <p><%= comment.body %></p>
  <div class="media-body">
    <small>
     <%= link_to comment.user.name, comment.user %> commented on <%= comment.created_at.strftime("%b %d, %Y at %I:%M%p") %>
      <% if policy(comment).destroy? %>
        | <%= link_to "Delete", [comment.post, comment], method: :delete, remote: true %>
      <% end %>
    </small>
  </div>
</div>