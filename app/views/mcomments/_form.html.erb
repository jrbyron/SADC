<% if current_user %>
  <h4>Add a comment:</h4>
  <%= form_for [@mpost, @mpost.mcomments.build] do |f| %>
    <%= f.label :body %>
    <%= f.text_field :body %>
    <%= f.submit %>
  <% end %>
<% end %>